# ─────────────────────────────────────────────────────────────────────────────
# FRONTEND — set these in Vercel dashboard under Project → Settings → Env Vars
# ─────────────────────────────────────────────────────────────────────────────
#
# Base URL of the deployed Java/Spring Boot backend (NO trailing slash, WITH /api suffix).
# IMPORTANT: Set this in Vercel → Settings → Environment Variables for Production.
# Without this, the app falls back to the hardcoded Railway URL in backend.ts.
#
# Railway backend (current):
VITE_API_URL=https://spring-boot-backend-production-13e6.up.railway.app/api
