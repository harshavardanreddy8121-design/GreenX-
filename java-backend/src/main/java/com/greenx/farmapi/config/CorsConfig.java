package com.greenx.farmapi.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS is configured centrally in FarmManagementApiApplication#corsConfigurer().
 * This class is intentionally left empty to avoid conflicting CORS registrations.
 */
@Configuration
public class CorsConfig {
}
